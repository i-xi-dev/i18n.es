// export class Locale {
//   readonly #resolvedTag: string;

//   private constructor(resolvedTag: string){
//     this.#resolvedTag = resolvedTag;
//   }

//   static resolve(tag: string): Locale {
//     let canonicalTag:string;
//     try {
//       canonicalTag=Intl.getCanonicalLocales(tag)[0];
//     }
//     catch {
//       throw new RangeError("tag");
//     }

//     const test = new Intl.DisplayNames(canonicalTag);
//   }
// }
